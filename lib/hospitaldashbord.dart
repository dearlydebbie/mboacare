import 'package:flutter/material.dart';

class HospitalDashbord extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is the Hospital Dashbord Page'),
      ),
    );
  }
}
